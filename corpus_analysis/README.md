# Corpus open-coding — P1.2 → P1.3 bridge

**Status as of Jul 20, 2026: DONE.** All 3,074 fields in the 116-company real-form corpus (from P1.2's harvester) have been classified — either by deterministic clustering, by the founder reading every field individually with live-verified raw-HTML evidence, or by high-confidence auto-match against an already-confirmed category. `needs_review` is 0.

This directory is the tracked, permanent home for that work. It is derived analysis of `corpus/manifest.jsonl` (which stays gitignored, per `.gitignore`'s "generated data, not source" — the raw crawl can be regenerated by re-running the harvester; this directory's category taxonomy and decisions cannot be regenerated automatically and must not be lost).

## Why this exists (read this first)

P1.2's corpus harvester produced 767 crawled job postings. A first pass tried to auto-cluster identical/near-identical fields (matching `id`, `autocomplete`, normalized label text) to build a category system without hand-tuning against individual forms (see `FORM_ENGINE_DESIGN.md` §3.6a for why that discipline matters — it's the whole reason the harvester exists instead of just extending `filler_utils.js`). That clustering caught real duplicates but left ~2,100 fields unclustered residue.

The founder pushed back hard on treating "auto-clustered" as "done": (1) auto-clustering only catches *exact* string matches, so semantically-identical-but-differently-worded fields (e.g. "How did you hear about us?" vs "Where did you see or hear about this job?") never merge automatically — that's a judgment call, not a string match; (2) several early "confirmed" clusters turned out to be wrong when checked against raw HTML (see "The department_interest/location_preference correction" below) — pattern-matching without evidence produces confident-looking mistakes. The resolution: **read every residue field individually, verify against raw saved HTML whenever the label is ambiguous, and only classify with real confidence** — explicitly not semantic/LLM guessing dressed up as classification. Deterministic clustering only for the mechanical, unambiguous cases (exact id/label/autocomplete matches); human judgment for everything else, evidenced by HTML, worked through in small batches the founder reviewed live in chat before anything was written to a file.

## Pipeline (run in this order if you need to regenerate)

```
1. auto_cluster.py           reads ../scratchpad/oc_compact_full.json (or wherever
                              the compact corpus lives) → writes clusters.json
                              {clusters: [...], residue: [...]}. Five deterministic
                              rules, in priority order: id attribute match,
                              autocomplete spec match, normalized label match,
                              section-as-fallback-label match, known-pattern match
                              (currently: react-select required-input shim).

2. apply_decisions.py        applies the founder's confirm/reject/special calls
                              onto clusters.json's clusters → writes
                              cluster_decisions.json. Keyed by "rule::key" (NOT
                              bare key — several normalized keys are produced by
                              more than one rule, e.g. "email" via both id-rule
                              and label-rule as separate clusters).

3. residue_triage.py         reads clusters.json's residue + cluster_decisions.json
                              → splits residue into auto_match (>=0.60 word-overlap
                              against an already-CONFIRMED category's example
                              labels — mechanical, not semantic) vs needs_review
                              (everything else) → writes residue_triage.json.

4. apply_triage_decisions.py bulk-confirms every auto_match entry (all were
                              spot-checked for false positives, none found) →
                              writes triage_decisions.json.

5. manual_singleton_tags.py  hand-tags a handful of one-off fields found early in
                              the process, before the batch-review workflow below
                              existed → writes manual_singleton_tags.json.

6. batch{1..5}_decisions.py  the actual bulk of the work. Each batch: pull the
                              next chunk of needs_review fields, the founder and
                              the agent read them together in chat (small
                              sub-chunks, confirmed one at a time), raw HTML
                              pulled from corpus/pages/*.html.gz whenever a label
                              was ambiguous or a cluster's real nature was in
                              doubt, THEN the agreed calls get written into a
                              batchN_decisions.py script → batchN_decisions.json.
                              Merge into manual_field_index_tags.json after each
                              batch, regenerate clusters.json's residue list
                              (subtract every manual-tag + cluster-member key from
                              the full field set), re-run residue_triage.py, repeat.

7. correction_department_location.py   a mid-session correction, not a numbered
                              batch — see below. Also merged into
                              manual_field_index_tags.json.

Final state: manual_field_index_tags.json (975 entries) + manual_singleton_tags.json
(8 entries) + cluster_decisions.json (144 clusters: 98 confirmed / 43 rejected / 3
special) + triage_decisions.json (43 auto-matched) together cover all 3,074 fields.
```

**If you need to re-run this pipeline** (e.g. the corpus grows with more harvested companies), the residue-regeneration step (subtract all manual-tag keys + all cluster-member keys from the full field set) is NOT automated as a single script — it was run ad hoc in the terminal each time a batch merged. If you're picking this up, either extract that into a real script first, or be careful to replicate it exactly (see any `batchN` merge step in the git history / session transcript for the exact snippet).

## Field key format

Every decision is keyed by `(ci, fi)` — index into the compact corpus array, NOT by label text or job_id. `ci` = index into the top-level company array of `oc_compact_full.json`, `fi` = index into that company's `fields` array. This is deliberate: label text repeats across companies/fields in ways that are NOT the same question (see the department_interest correction below), and even `(job_id, field_id_hash)` isn't always stable enough — positional `(ci, fi)` against the exact compact-corpus snapshot in this directory (`oc_compact_full.json`) is the only fully unambiguous key. **If `oc_compact_full.json` is regenerated from a re-run of the harvester, all `(ci, fi)` references in every decision file become invalid** — the array order is not guaranteed stable across harvester runs. Treat `oc_compact_full.json` in this directory as the frozen snapshot these decisions apply to.

## The category system

~110+ categories now exist, essentially all traceable to a specific real field the founder read. Categories are NOT centrally listed anywhere yet as a single canonical enum — the closest thing is `sorted(set(cluster_decisions.json's confirm.values())) ∪ set(all category values across manual_field_index_tags.json + manual_singleton_tags.json)`. **Building that canonical list + picking final names (some are working names, e.g. `pep_disclosure`, `attention_check`) is upstream work for P1.4, not done here.**

Notable categories that are NOT simple "ask the user a fact" fields — flag these for P1.4's interpreter design, they need special handling, not just a label:
- **`other_followup`** — not a topic, a structural relationship (free-text follow-up to a preceding "Other" choice). Needs to resolve by finding the nearest preceding field, not by reading its own label.
- **honeypot fields** (found via `manual_field_index_tags.json`, e.g. Ogilvy's `edit-url`/`edit-send`) — must be actively SKIPPED, never filled, confirmed via raw HTML (`tabindex="-1" aria-hidden="true"`, label literally instructs "leave this field blank").
- **the react-select required-input shim** (`known_pattern::react_select_required_shim`, 75 fields, 53 companies) — a library-injected hidden validation input trailing custom comboboxes, not askable, resolves itself once the combobox it shims is filled. See `FORM_ENGINE_DESIGN.md` §7.
- **required field + no resolvable profile value → abort the whole application**, don't guess or submit incomplete. General resolution-layer rule, not specific to any one category. See `FORM_ENGINE_DESIGN.md` §7.

## The department_interest / location_preference correction (read before trusting any "obviously real" checkbox_group cluster)

Early in this session, checkbox groups sharing literal words like "Marketing," "Administration," "Boston, Massachusetts" across companies (Anduril, Cannondesign, D2L, Stripe, GoStudent, Ogilvy, OneAcreFund) got merged into `department_interest`/`location_preference` on pattern alone. When one of these (d2l's "No Department", `name="filter-group-1_4"`) got checked against raw HTML out of caution, it turned out to be a **job-board search/filter widget**, not an application-form field. Checking the rest of that cluster found the SAME word ("Marketing") meant four completely different fake things across four companies (job-board filter ×3 different widget libraries, Cookiebot cookie-consent banner ×1) plus one real Greenhouse field (omnicomhealth's actual "Department" question). 43 of 45 originally-merged fields were wrong.

**Lesson, now a standing practice for the rest of this session:** a checkbox_group cluster sharing option text across companies is NOT evidence those companies have "the same real question" — office/department/employment-type filter widgets are extremely common on job-listing pages and produce exactly this false-positive pattern. Always check raw HTML (`corpus/pages/{job_id}.html.gz`) for the actual wrapping `<label>`/`<legend>`/`aria-label`/class names before trusting a cross-company checkbox-group match, even (especially) when the pattern "looks obviously real."

## Two extraction bugs found and documented (for whoever next touches the harvester or `filler_utils.js`)

1. **Malformed empty-record artifact.** `field_id_hash == '709446a2'`, `tag: None`, every other attribute null — appeared identically 12x across 12 different companies' pages. A harvester bug (trailing bad record), not real DOM content. All 12 rejected by hash match in `batch2_decisions.py`.
2. **Placeholder-as-real-label gap.** Several forms (Anduril/Gem-platform, Workato, Trade Republic) put the actual question text in the DOM's `placeholder` attribute while `label` extracted empty — e.g. Trade Republic's `id="question_29988745003"` had `label: ""` but `placeholder: "Are you available and in Berlin between 15 July and 15 Oct 2026?"`. Confirmed via raw HTML on multiple independent examples, not a one-off. `enrichField()`/`collectFields()` (or their eventual P1.4 successor) should fall back to `placeholder` when `label` is empty, similar to the already-implemented `section`-as-fallback logic in `auto_cluster.py`'s clustering rule (which only helps the *analysis*, not the live extension — the live extraction code in `extension/filler_utils.js` doesn't have this fallback yet either).

## Files in this directory

| File | What |
|---|---|
| `README.md` | This file. |
| `oc_compact_full.json` | Frozen snapshot of the corpus in compact form (label/id/itype/section/etc. per field, `containerHTML` stripped). All `(ci, fi)` keys below refer to THIS file's array indices. |
| `auto_cluster.py` | Deterministic clustering rules (id/autocomplete/label/section-fallback/known-pattern). Produces `clusters.json`. |
| `clusters.json` | `{clusters: [{cluster_id, rule, key, members: [{ci, fi, company, job_id}]}], residue: [{ci, fi, company, job_id}]}`. Currently residue is empty in spirit (43 auto-matched, tracked separately) — see `residue_triage.json`. |
| `apply_decisions.py` / `cluster_decisions.json` | Founder's per-cluster confirm/reject/special calls (98 confirmed, 43 rejected, 3 special), keyed `"rule::key"`. |
| `residue_triage.py` / `residue_triage.json` | Splits clusters.json's residue into auto_match (mechanical word-overlap, pre-confirmed) vs needs_review (now empty). |
| `apply_triage_decisions.py` / `triage_decisions.json` | Bulk-confirms all 43 auto_match entries. |
| `manual_singleton_tags.py` / `manual_singleton_tags.json` | 8 early hand-tagged one-off fields (617mediagroup). |
| `manual_field_index_tags.py` / `manual_field_index_tags.json` | **The bulk of the real work** — 975 individually-read-and-confirmed `(ci, fi)` → category/reject decisions, each with a `note` explaining the evidence. Accumulated across `batch1`–`batch5` + the department/location correction. |
| `batch{1..5}_decisions.py` / `.json` | Source scripts for each read-through session, in order. Kept separately (not just squashed into manual_field_index_tags.json) so the reasoning trail per batch stays inspectable. |
| `correction_department_location.py` / `.json` | The department_interest/location_preference correction (see above) — 43 reclassified fields. |
| `field_review_template.html` | The artifact source template (placeholders for the 7 data blobs above, gets string-replaced to build `field_review.html`). Has a Clusters tab, a Residue Review tab (auto-match vs needs-your-read), a raw Residue tab, and a Named Groups rollup. **Rebuilding it correctly requires literal string replacement, NOT `re.sub` with a replacement string** — `re.sub`'s replacement-string escape handling silently corrupts embedded JSON containing `\n`/`\1`-like sequences. See any batch's rebuild step for the exact working snippet. |
| `field_review.html` | The last-built artifact, for reference. The live published version is at the Artifact URL from this session (ask the user, or republish this file via the Artifact tool — same file path redeploys to the same URL if published from a continuation of this conversation; a fresh conversation mints a new URL). |

## Full-volume extension (2026-07-28→29) — DONE

The 767-job/116-company open-coding pass above has been extended to the FULL Greenhouse harvest (17,391-17,396 companies, ~633K fields — see `corpus/README.md`'s "Two harvest runs" for the harvest side). This was a real second pass, not just a bigger `--limit`: junk-page triage, a versioned identity key, a formalized canonical taxonomy (`taxonomy_v1.py`), LLM-assisted residue classification with raw-HTML verification on anything ambiguous, and — the part that actually matters for autofill — **held-out company-level generalization validation**. Full plan: `C:\Users\himan\.claude\plans\logical-noodling-honey.md` (approved plan, followed step by step).

**Headline result — held-out validation (the real test, not a footnote):**
20 companies (`heldout_validation_report.json`'s `heldout_companies`) were never touched during clustering, taxonomy-building, or any decision-writing. Running the finished taxonomy/clustering rules cold against their 38,623 fields:
- **99.0% coverage** (38,236/38,623 fields resolve to a decision using only that field's own signals)
- **100% correctness** on a 60-field manual-verification sample (0 errors)
- The unresolved 1% is characterized (all 387 fields manually read) as one-off, job-posting-specific skill-gate phrasing (e.g. "Do you have at least N years of experience with `<specific tech stack>`?", uniquely worded per req) — genuine long-tail natural-language variance, not a systematic taxonomy gap. See `heldout_validation_report.json` and `heldout_sample_for_manual_review.json`.

This is close to the overall in-sample coverage number below, meaning the taxonomy is not overfit to the companies it was built from — it generalizes to unseen forms. Scope note, stated explicitly: this measures taxonomy coverage/correctness on unseen forms, not interpreter/replay/live-autofill success (those are P1.3/P1.4/post-P1.5, separate layers that depend on this corpus but aren't produced by it).

**Overall numbers (full corpus, not held-out):**
- 622,995 fields across 3,702 deterministic clusters + 9,952 residue fields
- **99.2% final coverage** (617,962 fields resolved: 1,736 confirm decisions, 306 reject, 3 special)
- **97 distinct confirmed topic categories** (`taxonomy_v1.py`'s `TOPIC_CATEGORIES` + `PROPOSED_V2_ADDITIONS`, e.g. `consent_privacy_policy`, `consent_gdpr_notice`, `visa_type` — added deliberately during this pass, not auto-invented)
- Saturation: new categories were still being found through the residue-classification batches but at a steadily decreasing rate per batch — the vast majority of new label-rule gap decisions map onto ALREADY-EXISTING v1 categories rather than requiring new ones, which is itself the evidence of saturation (see `label_rule_gap_decisions_1.json` through `_10.json`)

**Key methodology notes for whoever extends this further:**
- Non-English fields (German/French/Spanish/Portuguese/Polish/Dutch/Japanese/Korean/Norwegian) are explicitly OUT OF SCOPE and excluded (`non_english_gap_exclusions_*.json`, 7 files) — but English-language fields that merely reference a non-US location (e.g. "Are you able to work from São Paulo?") are KEPT and classified normally. 2 genuinely bilingual fields (Sony) were kept since real English text was embedded alongside the other language.
- Two NEW deterministic-clustering false-merge bugs were found and fixed during this pass (on top of the original department_interest/location_preference lesson above): a React-Select "Select..." placeholder chrome false-merge (106,946 fields, incl. localized Korean/Japanese variants) and a Greenhouse `form-question_<jobid>` id-wrapper false-merge (5,516 fields / 709 distinct real questions). Both are the SAME failure shape as the original correction — generic chrome/wrapper text masquerading as a real shared question. See `ontology_debt.md` entry 3 for the historical record.
- A genuine identity-key bug was found: `field_id_hash` (hashed from `tag+htmlType+name+id+autocomplete+role`) is NOT always unique — it collided between an Anduril site-search box and the react-select-shim hash, since neither `placeholder` nor `req` feed the hash. Documented in `ontology_debt.md` entry 6.
- Remaining genuine ambiguities (not force-classified) are tracked in `ontology_debt.md` — read it before assuming a "missing" category is actually missing.

**Files added by this pass:** `oc_compact_full_v2.json` (full-volume compact corpus — **258MB, exceeds GitHub's 100MB limit, gitignored, NOT in the repo** — regenerate locally via `python build_compact_corpus_v2.py` before running anything that reads it, e.g. `auto_cluster_v2.py` or `heldout_validation.py`; source is `corpus/manifest.jsonl`, itself gitignored/regenerable via the harvester, see `corpus/README.md`), `auto_cluster_v2.py`/`clusters_v2.json` (full-volume clustering), `taxonomy_v1.py` (canonical enum), `ontology_debt.md`, `backfill_stable_keys.py`/`stable_keys_backfill.json` (versioned identity key for pre-existing decisions), `triage_real_vs_junk*.py`/`.json` (junk-page triage), `original_corpus_junk_exclusions.py` (27 retroactively-excluded companies from the ORIGINAL 767-job corpus), `apply_gap_decisions.py`/`cluster_decisions_v2.json` (the merged final decision set), `heldout_validation.py`/`heldout_validation_report.json`/`heldout_sample_for_manual_review.json` (Step 6).

## P1.3 — replay harness (done)

`replay.py` + `interpreter_baseline.py`. `replay.py` loads
`oc_compact_full_v2.json`, runs any `interpret(field) -> {category,
confidence, rule, key}` function over every field (default:
`interpreter_baseline`, swappable via `--interpreter MODULE`), and scores it
against `cluster_decisions_v2.json` via `interpreter_baseline.ground_truth_lookup()`
— kept as a separate function from `interpret()` on purpose (see its
docstring): they only share implementation today because the baseline *is*
the key-derivation logic that built the answer key; that won't stay true
once P1.4 introduces real multi-signal interpretation, and the report needs
to be able to tell "the interpreter got smarter" apart from "we silently
redefined ground truth."

`interpreter_baseline.py` is a direct, byte-identical port of
`auto_cluster_v2.py`'s deterministic rule-lookup (id → autocomplete → label
→ section_fallback → known_pattern → options), restructured to classify one
field instead of clustering a whole corpus. It's deliberately dumb — no new
interpretation logic, just "what does pure rule-lookup already get us,"
which is the number P1.4 needs to beat.

**Real bug found and fixed during this build's own verification pass**: an
early version of `candidate_keys()` stopped at the first rule producing any
non-None key, without checking whether that (rule, key) pair actually
formed a real cluster (`auto_cluster_v2.py` only clusters when ≥2 fields
share a key — a field with a globally-unique `id` never becomes an `id`
cluster and must fall through to try `label` next). This under-resolved by
~3,500 fields relative to the real pipeline. Caught because replay's
reported coverage came in under the known 99.2% reference figure — exactly
the kind of divergence the replay design exists to catch. Fixed by checking
candidate keys against the full `cluster_decisions_v2.json` key space
(confirm ∪ reject ∪ special) before treating a rule as resolved.

**Baseline report** (`replay_report.json`, full corpus, 632,947 fields):
99.25% of resolvable fields (topic + reject/special ÷ the 622,995 fields
`clusters_v2.json` actually clustered) match — consistent with, in fact
very slightly ahead of, the existing 99.19% README figure (the baseline
scores against the fully gap-decision-merged `cluster_decisions_v2.json`,
which resolves a few more keys than the intermediate clustering pass alone).
0% mismatch (expected — baseline and ground truth share derivation logic by
construction). 14,604 fields remain genuinely unresolved by any rule (the
corpus's own residual, same shape as the held-out validation's ~1% gap).
Confusion-matrix and interpreter-swap machinery independently verified
against a deliberately-corrupted scratch interpreter (forced
first_name/last_name swap) — correctly surfaced as the #1 and #2 confusion
pairs, ~19,300 each, then deleted (scratch-only, not a deliverable).

**Next: P1.4** — the real multi-signal interpreter. Every future rule
change gets scored by re-running `replay.py` against it; the KPI is
per-category coverage moving without the confusion matrix showing new
cross-category swaps.

**Before P1.3/P1.4 generalize past Greenhouse (still true, unstarted):** see `decisions/0008-corpus-harvester-scale-and-scope-gap.md` — the volume gap it originally flagged (767 vs. real job count) is now resolved for Greenhouse, and the OPEN-CODING gap (this file's old "Next step") is now also resolved (see above). The ATS-scope gap is NOT: `pipeline/corpus_harvester.py` still has Greenhouse-specific logic (iframe-embed detection) that won't transfer as-is to Ashby/Lever/SmartRecruiters — each needs its own "how do I reach the real rendered form" investigation, even though the field-discovery JS itself (`_EXTRACTION_JS`) likely generalizes unchanged. Flagged, not started.
