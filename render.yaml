services:
  - type: web
    name: persift-discovery
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile.discovery
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SLACK_WEBHOOK_URL
        sync: false
      - key: LOG_LEVEL
        sync: false
