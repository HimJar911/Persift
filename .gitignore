.env
*.db
*.pyo

# State files
persift_jobright_state.json
jobright_state.json
simplify_listings.json

# Output artifacts
outputs/*
!outputs/.gitkeep

# Corpus harvester output (P1.2) — generated crawl data, not source
corpus/*
!corpus/README.md

# Category-classification eval corpus (side mission, Jul 24) — generated batch/label
# churn, not source. See corpus_analysis/category_eval/README.md for what it is and
# how to regenerate/resume it.
corpus_analysis/category_eval/*
!corpus_analysis/category_eval/README.md
corpus_analysis/category_eval_v2/*
!corpus_analysis/category_eval_v2/README.md
!corpus_analysis/category_eval_v2/score_resolved_control.py

# Full-volume compact corpus (Jul 28-29 extension) — 258MB, exceeds GitHub's
# 100MB file limit. Regenerable from corpus/manifest.jsonl via
# build_compact_corpus_v2.py; not source. All decision files that key
# against it (cluster_decisions_v2.json, etc.) stay tracked.
corpus_analysis/oc_compact_full_v2.json

# Resume files (personal — not for repo)
resume/base_resume.pdf
resume/base_resume.txt
resume/base_resume.docx
resume/.resume_calibrated

# Python cache
__pycache__/
*.pyc

# Virtual env
.venv/

# Claude Code local config
.claude/

# Self-healing test pipeline (test_pipeline/) working/generated data — per
# joyful-sprouting-swan.md's "VM data lifecycle" decision, only code (git
# commits) and interpreter_regressions.json entries are permanent artifacts;
# everything below is throwaway working state, not a project deliverable.
test_pipeline_scratch/
test_pipeline_profiles/
failures/
checkpoints/
decision_agent/state/
