services:
  db:
    image: postgres:16
    container_name: persift-db
    environment:
      POSTGRES_DB: persift
      POSTGRES_USER: persift
      POSTGRES_PASSWORD: persift
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
